#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 코드 작성
    double a,b;
    cin >> a >> b;
    cout << fixed << setprecision(9) << a/b;
    return 0;
}