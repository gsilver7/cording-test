#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 코드 작성
    string s;

    while (getline(cin, s)) {
        cout << s << '\n';
    }
    return 0;
}