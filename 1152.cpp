#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);

    // 코드 작성
    string s;
    int count = 0;

    while(cin >> s){

        count+=1;
    }

    cout << count;

    return 0;
}